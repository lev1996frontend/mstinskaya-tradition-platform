fastapi==0.115.1
uvicorn[standard]==0.31.0
sqlalchemy==2.0.52
alembic==1.20.0
psycopg[binary]==3.3.5
pydantic==2.13.5
pydantic-settings==2.15.0
python-dotenv==1.2.3
PyJWT==2.14.0
bcrypt==5.0.0
pytest==9.1.1
pytest-asyncio==1.4.0
aiosqlite==0.22.1
email-validator==2.3.0
# .xlsx entry lists: the template the organizer downloads and the file they
# upload are read by one module, so the columns cannot drift apart.
openpyxl==3.1.5
# .docx entry lists: the same blank in Word, for clubs that fill in a document
# rather than a spreadsheet. Reads and writes one table shaped by the same
# column definition openpyxl uses.
python-docx==1.1.2
# Required by FastAPI for UploadFile; without it the intake router fails at
# import time, not at request time.
python-multipart==0.0.9
httpx==0.28.1
# Per-IP throttling on /api/v1/auth/* — see app/core/rate_limit.py.
slowapi==0.1.10
# Transactional email (verification, later password reset) — see app/core/email.py.
resend==2.44.0
