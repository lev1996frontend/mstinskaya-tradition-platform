fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.1
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1
PyJWT==2.9.0
bcrypt==4.1.2
pytest==8.3.3
pytest-asyncio==0.24.0
aiosqlite==0.20.0
email-validator==2.3.0
# .xlsx entry lists: the template the organizer downloads and the file they
# upload are read by one module, so the columns cannot drift apart.
openpyxl==3.1.5
# Required by FastAPI for UploadFile; without it the intake router fails at
# import time, not at request time.
python-multipart==0.0.9
httpx==0.27.2
